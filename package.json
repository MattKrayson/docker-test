{
  "name": "image-to-base64-converter",
  "version": "1.0.0",
  "description": "React + Express app to convert images to base64",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "client": "cd client && npm start",
    "build": "cd client && npm install && npm run build",
    "dev": "concurrently \"npm start\" \"npm run client\""
  },
  "keywords": [
    "express",
    "react",
    "docker",
    "image-converter",
    "base64"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
